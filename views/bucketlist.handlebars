<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_image}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}}</p>
  <p>
    <span for="img" aria-label=""></span>
  </p>
  <p>
    <button class="btn btn-primary">Start Your Travels!</button>
  </p>
</div>